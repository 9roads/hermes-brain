# Hermes Scheduler

Loisa does not ship scheduled Hermes jobs right now.

Do not add `jobs.json` or checked-in prompt templates unless Hermes should
start shipping profile-owned scheduled work again.

The Loisa Hermes image wrapper owns profile install/update, Composio CLI,
nori-slack CLI availability, OpenViking startup, and `nori-slack-cli` skill
verification.
