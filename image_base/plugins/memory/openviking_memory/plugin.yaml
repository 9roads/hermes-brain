name: openviking_memory
version: 0.2.0
description: "Phoenix general-purpose memory provider backed by OpenViking sessions, search, resources, and extraction."
pip_dependencies:
  - httpx
requires_env:
  - OPENVIKING_ENDPOINT
hooks:
  - on_session_end
  - on_session_switch
  - on_pre_compress
